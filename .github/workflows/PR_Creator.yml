# name: PR  Creator
# on: push
# jobs:
#   createPullRequest:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v2
#       - name: Set environment variables
#         run: |
#           echo ::set-env name=PULL_REQUEST_TITLE::"[Test] PR on $(date +%d-%m-%Y) No $(od -An -N2 -i /dev/random)"
          
#       # - name: Make changes to pull request
#       #   run: |
#       #     git config user.name 'pawarashish564'
#       #     git config user.email 'pawarashish564@gmail.com'
#       #     date +%s >> fileData.txt
#       #     export num=$(od -An -N2 -i /dev/random)
#       #     git add fileData.txt
#       #     git commit -m "commit $num"
#       - name: Create Pull Request
#         id: cpr
#         uses: peter-evans/create-pull-request@v3
#         with:
#           title: ${{ steps.vars.outputs.pr_title }}  
#           body: |
#             Update report
#             - Updated with *today's* date
#             - Auto-generated by PR_BOT

#           token: ${{ secrets.GITHUB_TOKEN }}
#           author: ${{ github.actor }} <${{ github.actor }}@users.noreply.github.com>
#           signoff: false
#           branch: example-patches-new
#           delete-branch: true
          
#           # [1]: https://github.com/peter-evans/create-pull-request
#           labels: |
#             report
#             automated pr
#           # assignees: pawarashish564
#           # reviewers: pawarashish564
#           # team-reviewers: |
#             # owners
#             # maintainers
#           # milestone: 1
#           draft: false

#       - name: Check output
#         run: |
#           echo "Pull Request Number - ${{ steps.cpr.outputs.pull-request-number }}"